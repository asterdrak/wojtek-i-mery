# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/asterdrak/#{repo_name}" }

gem "jekyll", "~> 3.8"
gem 'jekyll-paginate', group: :jekyll_plugins
